[package]
name = "factstore-memory"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
factstore = { path = "../factstore" }
serde_json = "1"

[dev-dependencies]
factstore-conformance = { path = "../factstore-conformance" }
